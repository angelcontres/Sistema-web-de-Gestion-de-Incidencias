# Bloqueo: ejemplo_nueva_feature

**Motivo:** Los criterios de aceptación de la feature `ejemplo_nueva_feature` en `feature_list.json` son insuficientes ("Criterio 1", "Criterio 2"). No es posible redactar requisitos (requirements) completos en formato EARS sin información detallada sobre lo que debe hacer esta feature.

**Acción requerida:** El humano debe clarificar los criterios de aceptación en `feature_list.json` o eliminar la feature si es solo un ejemplo de prueba.
